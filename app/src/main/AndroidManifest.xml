<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.adityaherlambang.bukuku">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:replace="android:icon">
        <activity android:name=".buku.EditBukuActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".buku.BukuActivity" />
        </activity>
        <activity android:name=".buku.BukuActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MenuActivity" />
        </activity>
        <activity android:name=".penerbit.EditPenerbitActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".penerbit.PenerbitActivity" />
        </activity>
        <activity android:name=".penerbit.PenerbitActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MenuActivity" />
        </activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".kategori.EditKategoriActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".kategori.KategoriActivity" />
        </activity>
        <activity android:name=".kategori.KategoriActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MenuActivity" />
        </activity>
    </application>

</manifest>